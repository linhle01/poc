import config from '../config/config.json';

export const base = {
  bgPrimary: config.themes.colors['bg-primary'],
  bgSecondary: config.themes.colors['bg-secondary'],
  bgHeader: config.themes.colors['bg-header'],
  bgFooter: config.themes.colors['bg-footer'],
  primary: config.themes.colors['primary'],
  secondary: config.themes.colors['secondary'],
  textPrimary: config.themes.colors['text-primary'],
};
